diff --git a/app/components/Menu.vue b/app/components/Menu.vue
index 614e897..6c37761 100644
--- a/app/components/Menu.vue
+++ b/app/components/Menu.vue
@@ -4,7 +4,7 @@ interface MenuItem {
   to: string
 }
 
-const defaultItems: MenuItem[] = [
+const FALLBACK_ITEMS: MenuItem[] = [
   { label: 'Home', to: '/' },
   { label: 'Über uns', to: '/ueber-uns' },
   { label: 'Services', to: '/services' },
@@ -14,12 +14,23 @@ const defaultItems: MenuItem[] = [
 const props = defineProps({
   items: {
     type: Array as PropType<MenuItem[]>,
-    default: () => defaultItems
+    default: () => FALLBACK_ITEMS
   }
 })
 
-const menuItems = computed(() => props.items)
+const menuItems = toRef(props, 'items') // toRef für reaktiven Zugriff auf Props
 const route = useRoute()
+
+const getItemClasses = (itemPath: string) => {
+  const baseClasses = 'px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200'
+  const activeClasses = 'bg-gray-900 text-white'
+  const inactiveClasses = 'text-gray-300 hover:bg-gray-700 hover:text-white'
+  const isActive = route.path === itemPath
+
+  return isActive
+      ? `${baseClasses} ${activeClasses}`
+      : `${baseClasses} ${inactiveClasses}`
+}
 </script>
 
 <template>
@@ -28,11 +39,7 @@ const route = useRoute()
       <li v-for="item in menuItems" :key="item.to">
         <NuxtLink
             :to="item.to"
-            :class="{
-            'px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200': true,
-            'bg-gray-900 text-white': route.path === item.to,
-            'text-gray-300 hover:bg-gray-700 hover:text-white': route.path !== item.to
-          }"
+            :class="getItemClasses(item.to)"
             aria-current="page"
         >
           {{ item.label }}
